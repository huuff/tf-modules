# S3 State
Please note that this module creates a S3 bucket with `prevent_destroy = true` so you'll never be able to remove it with terraform.

This is evil but I care a lot about my terraform state.
